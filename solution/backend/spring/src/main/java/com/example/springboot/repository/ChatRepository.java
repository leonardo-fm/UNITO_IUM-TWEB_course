package com.example.springboot.repository;

import com.example.springboot.model.Chat;

public class ChatRepository {

    public Chat GetPlayerChat(int playerId) {
        // Connect to Db and take data
        return new Chat();
    }

    public Chat GetClubChat(int playerId) {
        // Connect to Db and take data
        return new Chat();
    }

    public Chat GetCompetitionChat(int playerId) {
        // Connect to Db and take data
        return new Chat();
    }
}
